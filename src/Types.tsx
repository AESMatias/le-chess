export interface Coords {
    row: number;
    col: number;
}
export interface newRookPosition {
    row: number;
    col: number;
}

export interface newKingPosition {
    row: number;
    col: number;
}
